<div class="navbar navbar-fixed-top">
  <div class="navbar-inner">
    <div class="container">
      <a class="brand" href="#">CrowdFunder</a>
      <div class="nav-collapse collapse">
        <ul class="nav">
          <li class="<%= 'active' if current_page? root_path %>">
            <%= link_to 'Home', :root %>
          </li>
          <li class="<%= 'active' if controller.controller_name == "projects" %>">
            <%= link_to 'Projects', :projects %>
          </li>
        </ul>
      

      </div>
        <div class="pull-right">
        <% if logged_in? %>
        <%= link_to 'Logout', :logout, class: 'btn btn-warning' %>
         <% else %>
         <%= link_to 'Login', :login , class: 'btn btn-success' %>
         <%= link_to 'Sign Up', [:new, :user], class: 'btn btn-success' %>

        <% end %>
</div>
    </div>
  </div>
</div>

